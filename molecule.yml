---
docker:
  containers:

      - name: ansible-role-postman-01
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1

      - name: ansible-role-postman-02
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1